add guards for incidents list, edit, and view pages

hide the edit button inside incidents list if the user is not a Manager

add navigation buttons on the header

move REQUIRED_FILED_TXT and INVALID_EMAIL_TXT inside report-incident component to a global class/service

handle errors by status code inside incidents service

move apiBaseUrl inside incidents service to a json file

research how to conditionally add directives (or just the opening html tag) to refactor form fields with validations (form group
and form controls passed to their components)

add animation for dismissing toasts

remove console.log from all classes

modify sendRequest method in http-requests.service.ts to use http.request instead of calling http.post and http.get separately
code below implements the method correctly but for some reason the subscribers are getting called immediately:
const req = new HttpRequest(method, url, body, headers ? { headers } : {});
return this.http.request<any>(req).pipe(
    map((event: HttpEvent<any>) => {
    this.decrementOpenReqs();
    return '';
    }),
    catchError((err: HttpErrorResponse) => {
    this.decrementOpenReqs();
    return throwError(() => err);
    }),
);
